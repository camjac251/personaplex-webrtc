[project]
name = "moshi-personaplex"
requires-python = ">= 3.10"
description = "Personaplex finetune of moshi to enable role and voice control."
dependencies = [
    "numpy >= 1.26, < 2.2",
    "safetensors >= 0.4.0, < 0.5",
    "huggingface-hub >= 0.24, < 0.25",
    "einops == 0.7",
    "sentencepiece == 0.2",
    "sounddevice == 0.5",
    "sphn >= 0.1.4, < 0.2",
    "torch >= 2.4.0",
    "aiohttp>=3.10.5, <3.11",
    "aiortc >= 1.9, < 2",
    "av >= 12, < 14",
    "pyloudnorm >= 0.1.1, < 0.2",
    "accelerate >= 0.33.0",
]
authors = [{name="Rajarshi Roy", email="rajarshir@nvidia.com"}]
maintainers = [{name="Rajarshi Roy", email="rajarshir@nvidia.com"}]
license = {text = "MIT"}
dynamic = ["version"]
readme = "README.md"

[project.optional-dependencies]
# Optional second model for transcribing the user's microphone audio,
# enabled at runtime with --enable-asr. Kept out of the default
# dependencies so `uv sync --frozen` installs are unaffected; install
# with `uv pip install faster-whisper` (or `pip install moshi-personaplex[asr]`)
# only on hosts with the GPU headroom for a second model.
asr = ["faster-whisper>=1.0"]
# Optional CPU-only WavLM enrollment selector. The default install and
# `uv sync --frozen` do not install or download this model.
voice-selection = ["transformers>=4.43,<4.44"]

[project.scripts]
moshi-server = "moshi.server:main"
moshi-offline = "moshi.offline:main"

[tool.setuptools.dynamic]
version = {attr = "moshi.__version__"}

[tool.setuptools.package-data]
moshi = ["py.typed", "web_client/**/*"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
